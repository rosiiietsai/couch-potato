rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{user_id} {
      // logged in user uid must match the document's userId
      allow read,create, update, delete: if request.auth.uid == user_id;
    }
  }
}